package com.jpaver.trianglelist

import org.junit.Assert
import org.junit.Test

class RectangleTest {

    @Test
    fun testCalcPoint() {
        val p0 = PointXY(0.0f, 0.0f)
        //val rectangle = Rectangle( 3.0f, 4.0f, 5.0f, p0, 90.0f, 0 )
        /*Assert.assertEquals(rectangle.point[1].x, 0.0f, 0.001)
        Assert.assertEquals(rectangle.point[1].y, 3.0f, 0.001)
        Assert.assertEquals(rectangle.point[2].x, 4.0f, 0.001)
        Assert.assertEquals(rectangle.point[2].y, 3.0f, 0.001)
        Assert.assertEquals(rectangle.innerangle[0], 53.130f, 0.001)
        Assert.assertEquals(rectangle.innerangle[1], 90.000f, 0.001)
        Assert.assertEquals(rectangle.innerangle[2], 36.869f, 0.001)*/
    }

}