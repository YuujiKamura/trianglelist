{
  "name": "trianglelist-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync-wasm": "node scripts/sync-wasm.mjs",
    "dev": "npm run sync-wasm && vite",
    "build": "npm run sync-wasm && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "dxf-viewer": "^1.0.48"
  }
}
