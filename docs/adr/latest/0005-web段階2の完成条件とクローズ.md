# ADR 0005: Web 段階2 の完成条件を定義し、ここで閉じる

- 日付: 2026-06-10
- 状態: 採択 — 完成条件は 2e (ea56352) 時点で達成済み、段階2 はクローズ
- 関連: ADR 0003 (寸法 2 層位置決め)、insights #75 (robolectric-launch-smoke) / #76 (wasm wrapper stale)

## Context

Web 移植 (段階2a〜2e) は app の機能を順に埋める形で進んだが、「どこまで行ったら移植できたと言えるか」の終わりが言語化されておらず、2f、2g…と際限なく足せる構造だった (2026-06-10 yuuji「やりたいことを沢山言って、とりとめのない感じになっている」への応答として、終わりの定義を文書に固定する)。

## Decision

**完成条件 (1 行)**: app の基本動線 ── 入力・接続・寸法操作・書き出し・保存 ── が web で全部でき、出力が app と golden 一致すること。

この条件は以下で達成済み。段階2 はここで閉じ、以降の機能追加は新しい段階番号 (または ADR) で起こす。

| 動線 | commit | 内容 |
|---|---|---|
| 入力 (表編集) | 4eee826 (2a) | 編集・追加・削除→即再描画 |
| 書き出し | 94a6a64 (2b) | CSV/DXF/SFC、golden 同値テストで app と同一出力を固定 |
| 操作系 | 067f600 (2c) | ズーム・パン・タップ選択 (isCollide を共有) |
| 入力 (実動線) + 接続 + 保存 | e9da82f (2d) | 3行フォーム+FAB+タップ親指定+接続追加+undo+autosave |
| 寸法操作 | ea56352 (2e) | W/H フリップ+旗揚げ+番号サークル移動 (ADR 0003 式⊕override 二層) |
| 安全網 | bfe262d / b18f6be | 見た目 golden 2層 + エミュレータなし E2E (Robolectric+Roborazzi) |

## 残ギャップ (= 段階2 の外、着手は別途判断)

app の操作一覧 (app/src/main/res/layout/ の fab id 全数 grep、2026-06-10) と web/index.html の照合:

- **出力**: fab_xlsx / fab_pdf — web は CSV/DXF/SFC のみ
- **図形操作**: fab_rot_l / fab_rot_r (回転)、fab_deduction (控除)、fab_numreverse (番号正逆)、fab_nijyuualign
- **見た目**: fab_fillcolor (塗り色)、fab_texplus / fab_texminus (文字サイズ)
- **共有**: fab_share / fab_mail (web では別概念になる)
- 対象外 (dev 用): fab_debug / fab_testbasic / fab_dxfview
- 既知の質の課題: 旗揚げテキストが初期 fit の範囲外に出ることがある → LabelPlacer (ADR 0002/0003 の段階3) のスコープ

## Consequences

- 「Web 移植できた」の判定基準が文書に固定され、以降の追加要望はこの ADR を起点に新段階として切れる
- 次の幹の候補は 3 つ: (a) 残ギャップから選んで段階2f、(b) LabelPlacer (段階3、自動配置)、(c) ADR 0003 残段階 (Triangle の dimPoint キャッシュ引き剥がし)。どれを取るかは yuuji の判断待ちで、それまで新機能は積まない

## 付記 (2026-06-11): 残ギャップの大半を解消

yuuji 指示「控除図形の編集フロー再現 + エクセルエクスポート、PDF は不要、分担して並列で」を受け、
worktree worker 3 体の並列実装で以下を解消 (各 worker の検証 + main の統合再検証 + push 済み):

- ~~fab_rot_l / fab_rot_r~~ → eb8ad68 (ADR 0007、本日午前)
- ~~fab_xlsx~~ → eb9fc3a (ExcelJS、アプリ XlsxWriter 鏡写し、Heron は Excel 数式)
- ~~fab_deduction~~ → 89f4313 (控除モード + click-to-place、幾何は common の WebDeduction、
  DXF 一致テストで pin)
- ~~fab_fillcolor / fab_texplus / fab_texminus / fab_numreverse~~ → 914fcb5
  (fill プリミティブ新設で三角形塗りもアプリ忠実化)

残: fab_pdf (yuuji 明言で不要)、fab_share / fab_mail (web では別概念)、fab_nijyuualign、
旗揚げテキストの LabelPlacer (段階3 スコープ)。GitHub Pages リリース段取りは 60abc6a
(workflow_dispatch 起動、.github/workflows/deploy.yml)。

## 付記 (2026-06-12): 控除モード FAB・図面枠・Pages 初公開

- ~~fab_nijyuualign~~ → 914fcb5 系列で実装済みだった (fabNijyuu、rotateCurrentTriLCR 相当)。
  上の残リストは旧記述
- **控除モード中の FAB 実機能化** → 3deee6c。グレーアウトで逃げていた
  上下 (カレント控除切替+センタリング) / ペンシル (書換え) / rot (Box 個別回転、
  common に WebDeduction.rotateDeductionShape 新設) / ゴミ箱 (削除) / 全体表示
  (選択控除へ) を setDeductionFab:1029-1063 準拠で実装。グレーアウト残は web に
  控除機能が無い B/C・寸法・二重・塗り色のみ
- **図面枠の画面描画** → ae889d4。WebFrame.renderFrame が DrawingFileWriter.
  writeDrawingFrame を prim JSON に落とす (枠定義の複製ゼロ)。ヘッダの「図面枠」
  チェックで ON/OFF。副産物: parseHeader のメタ行誤読 (図面番号欄に "TextSize, 30"
  が印字、DXF/SFC にも乗っていた) を画面目視で発見し修正
- **GitHub Pages 初公開** (yuuji 指示「Pages公開して良さそうだな」) →
  https://yuujikamura.github.io/trianglelist/ 。environment protection で
  feature ブランチが拒否されたため deployment branch policy に追加して解決。
  公開後の実体検証 (wasm マジックバイト・headless 目視) 済み
- UX 細部: 一覧/カレント行の Enter ナビ (9beab33, 1fcd794)、初回起動を
  createNew 同等の 1 個スタートに (e247114)、新規作成で listAngle リセット
  (146732c)、全体フィットに画面比 12% マージン (4ce7612)

## 付記 (2026-06-12 ②): キャンバス内テキストの in-place 編集とヘッダ空白デフォルト

- **テキスト dblclick → 境界ボックス付き in-place 編集** (yuuji 要望「境界ボックスが
  表示されて、その中でテキストを書き換える操作が全体出来るように」)。書き戻し先は
  テキストの素性で分岐: 寸法 = 辺プール (不成立値は redraw 共通関門が図を据え置き警告)、
  控除 = dedLines の名前列 (infoStr の番号・寸法は導出値)、図面枠 = headerLines。
  旧「寸法 dblclick → 一覧セルへジャンプ」(2026-06-11) はこれに置換 — 表は
  syncEdgeInputs で追従するので機能は包含。実装は web/src/main.ts の
  textEditTarget / hitEditableText / openTextEditor
- **枠タイトル欄は field タグで特定** — WebFrame.FramePrimWriter が DrawingFileWriter
  の書込み座標 (paper cm アンカー) を鏡にして "field":"koujiname|rosenname|zumennum|
  gyousyaname" を prim に付け、**値が空欄でも emit** する。内容一致での特定は空白で
  成立しないため。pin は WebFrameTest.frame_tags_title_fields_and_emits_blank_cells
- **ヘッダはデフォルト空白 + ラベル付き直列化** (yuuji 指示「無題工事とか書くより
  空白にしておいて、枠内をダブルクリックすると書き換えられるように」)。空白の
  ヘッダ行は再読込で落ちて欄がずれるため、serializeState は app 完全形式と同じ
  `koujiname,<値>` のラベル付き 4 行で書く (CsvLoader.parseCSV:155 は元々この形式が
  入口。CsvCodec は preLines として素通しするので export 系も不変)
- **路線名のテーブル入力は input のたび redraw** — 図面枠 ON なら枠タイトルがタイプに
  追従する (yuuji 要望「テーブル上で書き換えた時にキャンバスに反映」)。一覧の辺セルが
  既に input → redraw 配線なのと同型
- 検証: Kotlin web 53/53、vitest 17/17、tsc/vite build green、headless 実操作 E2E
  (空欄 koujiname に書込み → 枠反映 / 路線名タイプ → 枠 2 箇所即追従 / 寸法 4.00→4.50
  → 形状再計算+表追従 / 99 → ⚠ 図は更新しません / 控除名 仕切弁→人孔 → infoStr 追従)
  + スクショ目視批評済み
